name: Test Self-Hosted Runner

on:
  push:

jobs:
  test:
    runs-on: [self-hosted, Linux, ARM64]

    steps:
      - name: Print hello
        run: echo "Hello from my self-hosted runner!" > hello.txt

      - name: Show runner details
        run: |
          hostname
          whoami
          uname -m
